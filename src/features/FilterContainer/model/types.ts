export enum OrderType {
  DESC = 'desc',
  ASC = 'asc'
}

export enum SortType {
  VIEWS = 'views',
  TITLE = 'title',
  CREATED = 'createdAt'
}

export enum ViewType {
  FULL = 'FULL',
  SHORT = 'SHORT'
}
